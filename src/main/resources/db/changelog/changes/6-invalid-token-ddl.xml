<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
                   xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                   xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                   https://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.10.xsd">

    <changeSet id="6-invalid-token-ddl" author="moaydogdu">
        <!-- =========================================== -->
        <!-- DDL of INVALID_TOKEN -->
        <!-- =========================================== -->
        <createTable tableName="INVALID_TOKEN">
            <column name="ID" type="VARCHAR(36)">
                <constraints primaryKey="true" nullable="false"/>
            </column>
            <column name="TOKEN_ID" type="VARCHAR(36)">
                <constraints nullable="false"/>
            </column>
            <column name="CREATED_AT" type="TIMESTAMP"/>
            <column name="CREATED_BY" type="VARCHAR(36)">
                <constraints nullable="false"/>
            </column>
            <column name="UPDATED_AT" type="TIMESTAMP"/>
            <column name="UPDATED_BY" type="VARCHAR(36)">
            </column>
        </createTable>
        <!-- =========================================== -->
        <!-- DDL of INVALID_TOKEN -->
        <!-- =========================================== -->
    </changeSet>

</databaseChangeLog>
